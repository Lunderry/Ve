local ReplicatedStorage = game:GetService("ReplicatedStorage")

local moduleConvertir = require(ReplicatedStorage.Module.ConversionUnidades)

local probar = {
	["Numero"] = 1,
	["Tipo"] = { "°F", "°K" },
}

print(moduleConvertir:Convertidor(probar))